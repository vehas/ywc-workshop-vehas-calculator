// console.log('a')
const add = (a, b) => (a + b)
const minus = (a, b) => (a - b)
const multiply = (a, b) => (a * b)
const divide = (a, b) => (a / b)

// console.log(add(0, 20))
module.exports = {
  add: add,
  minus: minus,
  multiply: multiply,
  divide: divide}
